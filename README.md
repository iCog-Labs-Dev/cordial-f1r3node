# Blocklace

Blocklace is a Rust project for building and experimenting with block-oriented logic.

## Getting Started

```bash
git clone https://github.com/Johnnas12/blocklace.git
cd blocklace
cargo build
cargo test
cargo run
```

## Development

- Requires Rust and Cargo
- Use `cargo fmt` and `cargo clippy` before committing